# E3 — Objective Ablation (RQ3)

NESS, arxiv, MCAR @ 80%. One SSL objective at a time + the combined default
(hist+path) + no-SSL + single-view. Under the locked config (SAGE-128, ppr view,
800 epochs). Metric: F1 + Acc.

**Source:** `experiments/e3_objective_ablation.sh` → `ogbn_benchmarks/results/e3_ablation_20260723_094037/` (summary.txt + per-config logs). Loss-curve analysis (E8-b) also drawn from these logs.

**Status:** DONE (single-seed) — SAGE-128, ppr view, 800 epochs, arxiv MCAR @ 80%.
Multi-seed (E2) still needed to confirm the ~0.01 gaps (within seed noise).

**Configs:** ssl_{centroid, stats, recon, hist, path, triplet, anchor},
ssl_hist_path (combined), no_ssl, single_view.

## Results (Test F1, no_ssl = 0.3748 baseline)

| Config | Val F1 | Test F1 | Test Acc | Δ vs no_ssl | type |
|---|---|---|---|---|---|
| **hist** | 0.3922 | **0.3932** | 0.6365 | **+0.018** | label-based |
| hist+path | 0.3878 | 0.3906 | 0.6330 | +0.016 | label + structural |
| hist+anchor | 0.3866* | 0.3882 | 0.6286 | +0.013 | label + structural |
| **anchor** | 0.3788 | 0.3860 | 0.6294 | +0.011 | structural, global, stochastic |
| centroid | 0.3752 | 0.3779 | 0.6200 | +0.003 | feature, static |
| path | 0.3714 | 0.3760 | 0.6213 | +0.001 | structural, local, stochastic |
| no_ssl | 0.3750 | 0.3748 | 0.6242 | — | — |
| single_view | 0.3696 | 0.3748 | 0.6184 | 0.000 | (contrastive/2-view off) |
| triplet | 0.3628 | 0.3637 | 0.6171 | −0.011 | structural, local, stochastic |
| recon | 0.3523 | 0.3513 | 0.6129 | −0.024 | feature, stochastic |
| stats | 0.3477 | 0.3500 | 0.6144 | −0.025 | feature, static |

*(hist+anchor val approximate.)*

## Story for the paper (three tiers)

1. **Feature-space objectives don't work.** recon (−0.024), stats (−0.025), centroid
   (+0.003) are all inert or harmful — redundant with FP-prefill + message-passing
   aggregation, which already summarize neighbor features.
2. **Structural objectives are the strongest label-free signal, and the winners are
   stochastic.** Among label-free objectives, the two best are **anchor** (+0.011) and
   **path** (+0.001) — both structural and stochastic (fresh samples each step); the
   global one (anchor) helps most. Stated as a *property the winners share*, not a law:
   stochasticity alone is not sufficient (triplet is stochastic yet hurts, −0.011), so
   we report it as a tendency and test it directly in E8.
3. **Label-based is best.** hist (+0.018) is the single strongest objective — it injects
   class information the structural/feature objectives cannot. Kept as an explicitly
   *semi-supervised* component (see note below; no labels needed at inference).

**Combos:** stacking a structural objective on hist does not beat hist alone
(hist 0.393 vs hist+path 0.391 vs hist+anchor 0.388) — within seed noise, so "≈", not
"worse". Suggests hist already captures much of what the structural objectives add.

**Contrastive / two-view machinery is inert here:** single_view (0.3748) = no_ssl
(0.3748) exactly → the second view + Barlow-Twins add ~0 at 80% missing on the fixed
SAGE encoder.

**Caveat:** single-seed; the ~0.01 gaps are within plausible noise. E2 (multi-seed) is
required before any of these are stated as firm results.

---

## Note for the paper — `hist` is semi-supervised, not label-free (and needs NO labels at test)

Some objectives are **label-free** (purely structural): `path`, `triplet`, `anchor`
predict quantities derived only from graph topology and node embeddings — they need
no labels, ever. In contrast, `hist` predicts the **class distribution of a node's
observable neighbors**, so it uses labels during training. This raises a fair question:
*if `hist` relies on labels, does it need labels at inference?*

**No.** All self-supervised objectives — `hist` included — are **training-only
auxiliary losses**. They shape the encoder's weights during training and are then
discarded. The inference path is only `z = encoder(x) → classifier(z) → prediction`;
the hist head, path head, edge decoder, and contrastive term are never evaluated at
test time. No labels are required at inference (beyond the ground-truth test labels
used solely to *score* F1/Acc).

**No leakage.** The `hist` target is built from **observable-node labels only**: the
label one-hot matrix is populated exclusively for observable nodes, so masked/val/test
nodes contribute zero to every neighbor-histogram. This is the same label access the
classification loss already uses — i.e. standard *semi-supervised* training, not a
leak of held-out labels.

**Consequence for framing.** `path`/`triplet`/`anchor` are label-free and thus apply
even when no labels exist anywhere. `hist` is **semi-supervised**: it depends on
training-time observable labels (which we have anyway) but needs **no labels at
inference**. We therefore report `hist` as a semi-supervised comparison point, and keep
the *label-free* structural objectives as the core of the method.