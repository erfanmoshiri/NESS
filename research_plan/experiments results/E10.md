# E10 — Convergence + Representation Visualization

Visual evidence for the E8 factors (uses data already logged).

- **Convergence curves (primary):** per-objective loss over epochs, `path` (helping)
  vs `centroid` (inert) on same axes. `centroid` plateaus by ~ep20 (redundant/saturated);
  `path` keeps decreasing (non-redundant, stays hard). Temporal evidence for the
  redundancy factor. Data in `training_log.jsonl` → essentially free.
- **t-SNE (deferred, only if time):** masked-node embeddings, NESS+SSL vs no-SSL,
  colored by class. Illustrates class-cluster separation. Qualitative; drop if the
  ~0.01 F1 effect isn't visibly clear.

**Status:** PENDING (loss curves = cheap; t-SNE = last).
