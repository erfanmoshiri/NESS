# E7 — View Analysis (RQ4)

Vary second contrastive view (edge_mask / dropout / feat_mask / ppr /
prefill_contrast / deep), objective fixed to hist+path. Which view best supports
the shared representation, and why. arxiv @ 60% (or 80%). F1 + Acc.

**Status:** PENDING — earlier run predates encoder fix + the ppr-on-raw change;
must rerun with SAGE encoder and current ppr (raw-diffusion) default.

<!-- Table: view2 | F1 | Acc -->
