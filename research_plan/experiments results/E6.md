# E6 — Scalability

Per-epoch + total wall-clock and peak GPU memory, all methods on arxiv.
Mark methods infeasible at scale (SVGA, SAT = OOM). Shows NESS scales where
prior imputation paradigms cannot.

**Status:** PENDING.

<!-- Table: model | s/epoch | total time | peak GPU mem | feasible? -->
